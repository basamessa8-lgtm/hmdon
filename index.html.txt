<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>لعبة المربع السريع</title>
  <style>
    body {
      font-family: Tahoma, sans-serif;
      text-align: center;
      background: #f0f0f0;
    }
    #gameArea {
      width: 600px;
      height: 400px;
      margin: 20px auto;
      border: 2px solid #333;
      position: relative;
      background: #fff;
    }
    .box {
      width: 50px;
      height: 50px;
      background: crimson;
      position: absolute;
      cursor: pointer;
    }
    #score {
      font-size: 20px;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <h1>لعبة المربع السريع</h1>
  <div id="gameArea"></div>
  <div id="score">النقاط: 0</div>

  <script>
    const gameArea = document.getElementById("gameArea");
    const scoreDisplay = document.getElementById("score");
    let score = 0;

    function createBox() {
      const box = document.createElement("div");
      box.classList.add("box");

      // مكان عشوائي داخل منطقة اللعبة
      const x = Math.random() * (gameArea.clientWidth - 50);
      const y = Math.random() * (gameArea.clientHeight - 50);

      box.style.left = x + "px";
      box.style.top = y + "px";

      // عند الضغط على المربع
      box.addEventListener("click", () => {
        score++;
        scoreDisplay.textContent = "النقاط: " + score;
        gameArea.removeChild(box);
      });

      gameArea.appendChild(box);

      // يختفي بعد ثانيتين إذا ما ضغطت عليه
      setTimeout(() => {
        if (gameArea.contains(box)) {
          gameArea.removeChild(box);
        }
      }, 2000);
    }

    // إنشاء مربع جديد كل ثانية
    setInterval(createBox, 1000);
  </script>
</body>
</html>

